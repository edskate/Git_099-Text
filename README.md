# Git_099-Text
- Comandos do git.

- git switch main
- git pull origin main

- git switch -c pagina_004

# altere o arquivo

- git status
- git add .
- git commit -m "feat: cria pagina 004"
- git push -u origin pagina_004


  # criando merge
  
- git pull origin main
- git merge --no-ff pagina_003 -m "merge: pagina 003 na main"
- git log --oneline --graph --all --decorate
- git push origin main

